{
  "scripts": {
    "dev": "live-server",
    "check:v2": "node scripts/check-v2.mjs",
    "playwright:install-chromium": "PLAYWRIGHT_BROWSERS_PATH=0 npx playwright install chromium",
    "check:assets": "node scripts/check-assets.mjs",
    "test:v4": "playwright test tests/v4-console.spec.js",
    "test": "npm run check:v2 && npm run check:assets && npm run test:v4"
  },
  "devDependencies": {
    "@gltf-transform/cli": "^4.3.0",
    "@playwright/test": "^1.58.2",
    "draco3dgltf": "^1.5.7",
    "live-server": "^1.2.2",
    "three": "^0.184.0"
  }
}
